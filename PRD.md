# PRD — 강북구 아파트 매매 대시보드

> "내 예산으로 강북구에서 뭘 살 수 있는가"에 답하는 탐색형 단일 페이지 대시보드.

---

## 1. 배경 & 페르소나

**사용자**: 투자·시장 분석가 관점으로 강북구를 보는 사람.
숫자를 눈으로 훑고 판단하되, 출발점은 **자기 예산**이다. 감상이 아니라 실제 거래 사실로 답해주기를 기대한다.

**핵심 질문 (한 문장)**
> **내 예산으로 강북구에서 뭘 살 수 있는가?** — 그리고 그 예산은 이 동네에서 어느 정도 위치인가?

대시보드는 **결론을 통보하는 화면이 아니라, 사용자가 예산을 넣고 답을 찾아가는 화면**이다.
탐색 결과를 해석할 맥락(구 순위·추세·동별 격차)은 그 아래에 근거로 배치한다.

**대상 데이터**: `seoul-apt-latest.csv` 중 **매매 거래만** (강북구 1,490건 / 서울 전체 72,459건, 2025-07 ~ 2026-06)

---

## 2. 데이터 검증 결과 (기획 단계에서 실측 완료)

PRD의 모든 지표는 실제 데이터로 계산해 성립을 확인했다.

| 지표 | 실측값 |
|---|---|
| 강북구 매매 건수 | 1,490건 (12개월) |
| 단지 수 | 93개 (**단지→동은 1:1**, 여러 동에 걸친 단지 0개) |
| 거래가 분포 | 최저 8,000 / p25 57,500 / **중위 70,000** / p75 81,250 / 최고 127,000 (만원) |
| 중위 평당가 | 3,039만원/평 — **서울 25개 구 중 23위** |
| 반기 변화율 | **-1.33%** (25개 구 중 21위) |
| 거래량 변화 | 상반기 582건 → 하반기 908건 (**+56.0%**) |
| 동 구성 | 미아동 1,041 / 번동 252 / 수유동 180 / 우이동 17 |
| 동별 격차 | 미아동 3,273 vs 우이동 1,927 = **1.70배** |
| 평당가 결측 | **0건** |

### 2.1 치명적 함정 ① — 중위값으로 예산을 거르면 틀린다

**예산 탐색은 반드시 개별 거래가 기준이어야 한다.**

7억 예산으로 검증한 결과, **중위가는 7억을 넘지만 실제 7억 이하 거래가 존재하는 단지가 8개** 있었다.
그중에는 **거래량 1위 단지(에스케이북한산시티, 260건)** 도 포함된다 — 중위 70,925만원이지만 최저 47,600만원.

| 단지 | 최저가 | 중위가 |
|---|---|---|
| 에스케이북한산시티 | 47,600 | 70,925 |
| 경남아너스빌(1356-0) | 52,000 | 80,000 |
| 삼각산아이원 | 55,000 | 73,700 |
| 두산위브트레지움 | 65,000 | 85,700 |

> **설계 규칙**: 단지의 "매수 가능 여부"는 **중위가가 아니라 예산 이하 실거래 존재 여부**로 판정한다.
> 이를 위해 **개별 거래 1,490건을 클라이언트로 전송**한다 (압축 시 약 45KB — 정적 호스팅에 무리 없음).

예산별 도달 범위 (실측):

| 예산 | 거래 커버리지 | 단지 |
|---|---|---|
| 4억 | 112건 (7.5%) | 36개 |
| 5억 | 227건 (15.2%) | 58개 |
| 6억 | 461건 (30.9%) | 73개 |
| **7억 (중위)** | **753건 (50.5%)** | **82개** |
| 8억 | 1,105건 (74.2%) | 89개 |
| 10억 | 1,408건 (94.5%) | 91개 |

### 2.2 치명적 함정 ② — 구성효과(mix effect)

서울 전체 중위 평당가는 4,513 → 4,221만원(**-6.47%**)으로 하락한 것처럼 보이지만,
**개별 25개 구 중 18개 구는 상승**했다. 하반기에 저가 지역 거래 비중이 늘어 전체 중위값만 끌어내린 구성효과다.

> **설계 규칙**: "서울 평균 대비 강북구" 형태의 비교는 **쓰지 않는다.**
> 상대 위치는 **항상 25개 구 중 순위(n위/25)** 로만 표현한다. 순위는 거래 구성 변화에 영향받지 않는다.

### 2.3 표본 규칙 — 탐색과 랭킹을 구분한다

두 가지는 성격이 다르므로 규칙도 달라야 한다.

| 용도 | 규칙 | 근거 |
|---|---|---|
| **탐색** ("이 예산으로 살 수 있나") | **93개 단지 전부 포함** | "그 가격에 실제로 거래됐다"는 **사실**이지 추정이 아니다. 표본이 1건이어도 유효한 정보다. |
| **랭킹/대표값** (중위 평당가 비교) | `n >= 10`이 아닌 단지는 **대표값에 경고 배지** | 중위값은 **추정**이므로 표본이 적으면 신뢰할 수 없다. |
| 우이동 (n=17) | 차트에 "표본 17건" 경고 배지 | 동 단위 대표값의 신뢰도 문제 |
| 월별 추세 | **3개월 이동 중위값**으로 평활 | 월 표본이 70~203건으로 변동이 큼 |
| 대표값 | 평균이 아닌 **중위값(median)** | 고가 이상치에 견고 |

---

## 3. 화면 구성

세로 1페이지 스크롤. **"내 예산 → 결과 → 맥락"** 순서.

### S0. 예산 입력 + 즉답 ⭐ 화면의 주인공
페이지를 연 순간 **입력창이 먼저 보이고**, 조작하면 즉시 답이 바뀐다.

- **예산 컨트롤**: 슬라이더 + 숫자 입력 동기화. 범위 **1억 ~ 13억**, 1,000만원 단위, **기본값 7억**(강북구 중위가).
- **즉답 헤드라인**: `7억이면 강북구 아파트 거래의 50.5%, 82개 단지에 닿습니다` (예산에 따라 실시간 변경)
- **KPI 카드 4개** (모두 예산에 연동):
  1. **매수 가능 단지** 82개 / 93개
  2. **예산 이하 거래** 753건 (50.5%)
  3. **예산 내 최대 면적** N㎡
  4. **선택지가 가장 많은 단지** (예산 이하 거래 건수 1위)

> 예산이 너무 낮아 결과가 0건이면, 최저 거래가를 안내하는 빈 상태 메시지를 표시한다.

### S1. 가격 분포 속 내 예산의 위치
- **히스토그램** — 강북구 매매가 분포, 예산 지점에 **세로 기준선**을 긋고 예산 이하 구간을 primary 색으로 강조
- 캡션: `내 예산은 강북구 거래의 하위 50.5% 지점입니다`

### S2. 예산으로 살 수 있는 단지 ⭐ 핵심 결과 테이블
- **예산 이하 거래가 1건 이상인 단지만** 표시 (§2.3 — 93개 전부가 후보)
- 컬럼: 순위 · 단지명 · 동 · **최저가** · **예산 이하 거래수 / 전체** · 중위 평당가 · 대표 면적
- 기본 정렬: **예산 이하 거래수 내림차순** (선택지가 많은 곳이 위로)
- 모든 컬럼 정렬 가능 → 중위 평당가로 정렬하면 기존의 "단지 랭킹" 관점이 그대로 나온다
- `n < 10` 단지의 중위 평당가에는 경고 배지 (§2.3)

### S3. 서울 25개 구 안에서의 위치
- **가로 막대 랭킹 차트** — 25개 구 중위 평당가 내림차순, 강북구만 강조
- 캡션: 1위 강남구(10,750) 대비 강북구는 **28% 수준** · `23위 / 25`

### S4. 12개월 추세 — 가격은 정체, 거래는 증가
- **복합 차트**: 막대(월별 거래량) + 라인(3개월 이동 중위 평당가), 이중 축
- 캡션: 가격은 -1.33%인데 거래량은 +56% — **거래는 살아났지만 가격은 따라오지 않은 구간**

### S5. 동별 격차
- **막대 차트** — 4개 동 중위 평당가 (미아 3,273 / 번 2,746 / 수유 2,613 / 우이 1,927)
- 거래 건수 병기, 우이동에 `표본 17건` 경고 배지
- 미아동이 거래의 70%를 차지 → **강북구 시세 ≈ 미아동 시세**임을 명시

### S6. 면적대별 비교
- 소형(~60㎡) 3,634 / 중형(60~85㎡) 3,066 / 대형(85㎡~) 2,644 만원/평
- 인사이트: **작을수록 평당가가 비싸다**

---

## 4. 인터랙션

| 컨트롤 | 동작 | 영향 범위 |
|---|---|---|
| **예산 슬라이더/입력** ⭐ | 1억~13억, 기본 7억 | **S0, S1, S2** 실시간 갱신 |
| **동 필터** (전체·미아·번·수유·우이) | 선택 동으로 한정 | S0, S1, S2, S6 |
| **면적대 필터** (전체·소형·중형·대형) | 선택 면적대로 한정 | S0, S1, S2 |
| **테이블 정렬** | 컬럼 헤더 클릭 → 오름/내림 토글 | S2 |

- 슬라이더 조작 중에도 끊김이 없어야 한다 (1,490건 클라이언트 필터링이므로 충분히 가능)
- 필터 조합 결과가 0건이면 **빈 상태 메시지**를 명시적으로 표시
- S3~S5는 강북구 전체 맥락이므로 예산 필터의 영향을 받지 않는다 (혼동 방지를 위해 각 섹션에 "예산과 무관" 표기)

---

## 5. 기술 설계

### 5.1 제약
**GitHub Pages = 순수 정적 호스팅.** 서버·DB·API 없음. 27MB CSV를 브라우저가 직접 읽는 것은 불가.

### 5.2 아키텍처

```
seoul-apt-latest.csv (27MB, 320k행)
        │
        ▼  build_data.py  (Python 표준 라이브러리만, 빌드 타임 1회)
        │
data/dashboard.js  (~60KB)   window.DASHBOARD_DATA = {...}
        │  = 집계 지표 + 강북구 매매 개별 거래 1,490건(압축 배열)
        ▼  <script src>  ← fetch가 아님: file://에서도 동작
index.html + assets/dashboard.{css,js}
        │
        ▼
GitHub Pages
```

**핵심 결정 2가지**
1. **집계는 빌드 타임에, 탐색은 런타임에.** 25개 구 랭킹·추세 같은 무거운 집계는 Python이 미리 끝내고, 예산·동·면적대 조합처럼 **사용자 입력에 따라 달라지는 계산은 개별 거래 1,490건을 클라이언트에서 직접 필터링**한다. 사전집계로는 임의 예산에 정확히 답할 수 없다 (§2.1).
2. **데이터는 `.js`로 전달.** `fetch()`는 `file://`에서 CORS로 차단되므로 `<script src>`로 로드해 로컬·배포 양쪽에서 동작시킨다.

### 5.3 `data/dashboard.js` 스키마

```jsonc
{
  "meta":  { "gu": "강북구", "period": "2025-07~2026-06", "total": 1490 },
  "kpi":   { "medianPpp": 3039, "pppRank": 23, "pppTotal": 25,
             "changePct": -1.33, "changeRank": 21,
             "deals": 1490, "dealsChangePct": 56.01, "dongGapRatio": 1.7,
             "medianPrice": 70000, "minPrice": 8000, "maxPrice": 127000 },

  // ── 탐색용: 개별 거래 (압축 배열) ──────────────────────
  "complexList": [ { "name": "송천센트레빌", "dong": "미아동", "n": 18, "medianPpp": 4548 }, ... ], // 93개
  "months":      [ "2025-07", ..., "2026-06" ],                                                   // 12개
  "dealFields":  ["c", "m", "area", "floor", "price", "ppp"],   // 각 거래 배열의 의미
  "deals":       [ [12, 5, 84.8, 4, 103500, 4548], ... ],       // 1490행
                 // c = complexList 인덱스, m = months 인덱스
                 // 단지→동은 1:1이므로 동은 complexList에서 조회 (§2 검증)

  // ── 맥락용: 사전집계 ────────────────────────────────
  "guRanking":     [ { "gu": "강남구", "medianPpp": 10750, "n": 2933 }, ... ],   // 25개
  "monthly":       [ { "ym": "2025-07", "deals": 78, "medianPpp": 2910, "ma3": null }, ... ],
  "dongs":         [ { "dong": "미아동", "medianPpp": 3273, "n": 1041, "lowSample": false }, ... ],
  "areaBands":     [ { "band": "소형(~60㎡)", "medianPpp": 3634, "n": 367 }, ... ],
  "dongAreaBands": { "미아동": [ { "band": "소형(~60㎡)", "medianPpp": 4214, "n": 184 }, ... ], ... }
}
```

> `complexes[].byArea`는 더 이상 필요 없다 — 개별 거래에서 직접 계산하는 쪽이 정확하고 유연하다.

### 5.4 프론트엔드

- **차트: 외부 라이브러리 없이 인라인 SVG로 직접 구현.** CDN 의존 없음, Montage 토큰을 차트 내부까지 100% 적용.
  차트 종류 — 히스토그램(S1), 가로 막대(S3), 막대+라인 이중축(S4), 세로 막대(S5·S6)
- **디자인**: Wanted Montage 토큰 승계·확장. 라이트/다크 모드 양쪽 지원.
- **상승/하락 표기**: 색으로 구분하지 않는다(한국·서구 관행 충돌). **화살표(▲/▼) + 부호(+/-)** 로 표기하고 색은 primary/neutral만 사용.
- **반응형**: 375px에서 본문 가로 스크롤 없음. 넓은 표·차트는 자체 `overflow-x:auto` 컨테이너 안에서만 스크롤.
- **접근성**: 차트에 `role="img"` + 한국어 `aria-label`. 예산 슬라이더는 `<input type="range">` + `aria-valuetext`(예: "7억원"). 표는 실제 `<table>` 시맨틱 유지.

### 5.5 파일 구성

```
sesac-web/
├── index.html              # 대시보드
├── profile.html            # 자기소개 (보존)
├── build_data.py           # CSV → data/dashboard.{json,js}
├── analyze.py              # 기존 유지
├── data/dashboard.js       # window.DASHBOARD_DATA (실제 로드 대상)
├── data/dashboard.json     # 동일 내용, 검사·디버깅용
├── assets/dashboard.css
├── assets/dashboard.js
├── seoul-apt-latest.csv
└── PRD.md
```

---

## 6. 성공 기준

| # | 기준 | 검증 방법 |
|---|---|---|
| 1 | **예산 필터가 개별 거래 기준으로 정확** — 7억 입력 시 82개 단지 / 753건 | CSV 직접 재계산과 대조 |
| 2 | **중위가 초과 단지도 포함** — 7억에서 에스케이북한산시티가 결과에 있어야 함 (§2.1) | 화면 확인 |
| 3 | 예산 슬라이더 조작 시 S0·S1·S2가 **끊김 없이** 갱신 | 조작 후 값 확인 |
| 4 | 화면의 모든 숫자가 CSV 재집계 결과와 **일치** | 산출값 ↔ 화면 대조 |
| 5 | "서울 평균 대비" 문구가 **어디에도 없음** (§2.2) | 텍스트 검색 |
| 6 | 라이트/다크 양쪽에서 글자가 읽힘 | 양 모드 확인 |
| 7 | 375px에서 **가로 스크롤 없음** | 뷰포트 테스트 |
| 8 | 데이터 파일 **100KB 이하**, 외부 리소스 0개 | 파일 크기 · grep |
| 9 | GitHub Pages 배포 URL이 200 응답 | curl |

---

## 7. 범위 밖

- 전세·월세 (매매만)
- 강북구 외 구의 심층 페이지 (강북구 고정)
- 지도 시각화 (좌표 데이터 없음)
- 개별 거래를 그대로 나열하는 원장형 테이블 — 거래 데이터는 **탐색 연산의 근거로만** 쓰고, 화면 표시 단위는 **단지**까지다 (합의된 깊이)
- 가격 예측·추천 (12개월 데이터로는 근거 부족)
