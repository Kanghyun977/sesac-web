# PRD — 강북구 아파트 매매 대시보드

> 서울 부동산 실거래 데이터로 "우리 동네(강북구)는 지금 어떤가"를 판정하는 단일 페이지 대시보드.

---

## 1. 배경 & 페르소나

**사용자**: 투자·시장 분석가 관점으로 강북구를 보는 사람.
숫자를 눈으로 훑고 "지금 여기가 어떤 상태인가"를 30초 안에 판정하고 싶어 한다. 감상이 아니라 **순위와 변화율**로 말해주기를 기대한다.

**핵심 질문 (한 문장)**
> 강북구는 서울 안에서 어디쯤이고, 지난 1년 동안 어느 방향으로 움직였는가?

**대상 데이터**: `seoul-apt-latest.csv` 중 **매매 거래만** (강북구 1,490건 / 서울 전체 72,459건, 2025-07 ~ 2026-06)

---

## 2. 데이터 검증 결과 (기획 단계에서 실측 완료)

PRD의 모든 지표는 실제 데이터로 계산해 성립을 확인했다.

| 지표 | 실측값 |
|---|---|
| 강북구 매매 건수 | 1,490건 (12개월) |
| 중위 평당가 | 3,039만원/평 — **서울 25개 구 중 23위** |
| 반기 변화율 | **-1.33%** (25개 구 중 21위) |
| 거래량 변화 | 상반기 582건 → 하반기 908건 (**+56.0%**) |
| 동 구성 | 미아동 1,041 / 번동 252 / 수유동 180 / 우이동 17 |
| 동별 격차 | 미아동 3,273 vs 우이동 1,927 = **1.70배** |
| 랭킹 가능 단지 | 총 93개 중 **거래 10건 이상 27개** |
| 평당가 결측 | **0건** (전 구간 사용 가능) |

### 2.1 반드시 피해야 할 함정 — 구성효과(mix effect)

서울 전체 중위 평당가는 4,513 → 4,221만원(**-6.47%**)으로 하락한 것처럼 보이지만,
**개별 25개 구 중 18개 구는 상승**했다. 하반기에 저가 지역 거래 비중이 늘어 전체 중위값만 끌어내린 구성효과다.

> **설계 규칙**: "서울 평균 대비 강북구" 형태의 비교는 **쓰지 않는다.**
> 강북구의 상대 위치는 **항상 25개 구 중 순위(n위/25)** 로만 표현한다.
> 순위는 거래 구성 변화에 영향받지 않아 안전하다.

### 2.2 표본 부족 처리 규칙

| 대상 | 규칙 |
|---|---|
| 우이동 (n=17) | 차트에는 포함하되 **"표본 부족" 배지**를 병기 |
| 단지 랭킹 | **거래 10건 이상(27개)만** 랭킹에 노출 |
| 월별 추세 | 월 표본이 70~203건으로 변동이 커, **3개월 이동 중위값**으로 평활 |
| 대표값 | 평균이 아닌 **중위값(median)** 사용 — 고가 이상치에 견고 |

---

## 3. 화면 구성

세로 1페이지 스크롤. 위에서 아래로 **"결론 → 근거 → 상세"** 순서.

### S0. 히어로 — 한 줄 판정 + KPI 4장
페이지를 연 순간 결론이 보인다.

- 헤드라인: `강북구는 서울에서 23번째로 싼 구입니다`
- 서브: 기간·건수·데이터 출처 명시
- KPI 카드 4개:
  1. **중위 평당가** 3,039만원/평 · `서울 23위/25`
  2. **반기 변화율** -1.33% · `21위/25` (하락 → negative 색상)
  3. **거래량** 1,490건 · `하반기 +56%` (증가 → positive 색상)
  4. **동별 격차** 1.70배 · `미아동 ↔ 우이동`

### S1. 서울 25개 구 안에서의 위치
- **가로 막대 랭킹 차트** — 25개 구 중위 평당가 내림차순
- 강북구 막대만 primary 색으로 강조, 나머지는 중립색
- 각 막대에 구 이름 + 값 라벨
- 캡션: 1위 강남구(10,750) 대비 강북구는 **28% 수준**

### S2. 12개월 추세 — 가격은 정체, 거래는 증가
- **복합 차트**: 막대(월별 거래량) + 라인(3개월 이동 중위 평당가), 이중 축
- 12개 시점 (2025-07 ~ 2026-06)
- 캡션: 가격은 -1.33%로 사실상 제자리인데 거래량은 +56% — **거래는 살아났지만 가격은 따라오지 않은 구간**

### S3. 동별 격차
- **막대 차트** — 4개 동 중위 평당가 (미아 3,273 / 번 2,746 / 수유 2,613 / 우이 1,927)
- 각 막대에 거래 건수 병기, 우이동에 `표본 17건` 경고 배지
- 보조: 동별 거래량 비중 (미아동이 70%를 차지 → 강북구 시세 ≈ 미아동 시세라는 사실 명시)

### S4. 단지 랭킹 (거래 10건 이상 27개)
- **정렬 가능한 테이블**
- 컬럼: 순위 · 단지명 · 동 · 중위 평당가 · 거래 건수 · 중위 거래가
- 기본 정렬: 중위 평당가 내림차순
- 상위 3위는 순위 칩 강조 (1위 송천센트레빌 4,548만원/평)

### S5. 면적대별 비교
- 소형(~60㎡) 3,634 / 중형(60~85) 3,066 / 대형(85~) 2,644 만원/평
- 인사이트: **작을수록 평당가가 비싸다** (소형이 대형보다 37% 높음)

---

## 4. 인터랙션

| 컨트롤 | 동작 | 영향 범위 |
|---|---|---|
| **동 필터** (전체·미아·번·수유·우이) | 선택 동으로 한정 | S4 단지 랭킹, S5 면적대 |
| **면적대 필터** (전체·소형·중형·대형) | 선택 면적대로 한정 | S4 단지 랭킹 |
| **테이블 정렬** | 컬럼 헤더 클릭 → 오름/내림 토글 | S4 |

- 필터 조합 결과가 **0건이면** 빈 상태 메시지를 명시적으로 표시
- 필터 변경 시 표본이 10건 미만으로 떨어지는 단지는 자동 제외 (§2.2 규칙 유지)

---

## 5. 기술 설계

### 5.1 제약
**GitHub Pages = 순수 정적 호스팅.** 서버·DB·API 없음. 27MB CSV를 브라우저가 직접 읽는 것은 불가.

### 5.2 아키텍처

```
seoul-apt-latest.csv (27MB, 320k행)
        │
        ▼  build_data.py  (Python 표준 라이브러리만, 빌드 타임 1회 실행)
        │
data/dashboard.json (~50KB, 사전집계)
        │
        ▼  fetch()
index.html + assets/dashboard.js + assets/dashboard.css
        │
        ▼
GitHub Pages
```

**핵심 결정**: 집계를 **빌드 타임에 Python으로** 끝내고, 브라우저에는 완성된 소형 JSON만 보낸다.
→ 27MB 다운로드 및 클라이언트 집계가 사라져 로딩이 즉시 끝난다.

### 5.3 `data/dashboard.json` 스키마

```jsonc
{
  "meta": { "gu": "강북구", "period": "2025-07~2026-06", "total": 1490, "generatedFrom": "seoul-apt-latest.csv" },
  "kpi": {
    "medianPpp": 3039, "pppRank": 23, "pppTotal": 25,
    "changePct": -1.33, "changeRank": 21,
    "deals": 1490, "dealsChangePct": 56.0,
    "dongGapRatio": 1.70
  },
  "guRanking": [ { "gu": "강남구", "medianPpp": 10750, "n": 2933 }, ... ],   // 25개
  "monthly":   [ { "ym": "2025-07", "deals": 78, "medianPpp": 2910, "ma3": null }, ... ],  // 12개
  "dongs":     [ { "dong": "미아동", "medianPpp": 3273, "n": 1041, "lowSample": false }, ... ],
  "complexes": [ { "complex": "송천센트레빌", "dong": "미아동", "medianPpp": 4548, "n": 18, "medianPrice": 121500,
                   "byArea": { "소형": {...}, "중형": {...}, "대형": {...} } }, ... ],  // n>=10, 27개
  "areaBands": [ { "band": "소형(~60㎡)", "medianPpp": 3634, "n": 367 }, ... ]
}
```

> 동·면적대 필터가 클라이언트에서 동작하려면 단지별 세부 집계가 필요하므로, `complexes[].byArea`에 면적대별 하위 집계를 함께 담는다.

### 5.4 프론트엔드

- **차트: 외부 라이브러리 없이 인라인 SVG로 직접 구현.**
  이유 — (a) Montage 디자인 토큰을 차트 내부까지 100% 적용 가능, (b) CDN 의존/버전 리스크 없음, (c) 필요한 차트가 가로막대·세로막대·라인 4종으로 단순, (d) 정적 호스팅에서 최속 로딩.
- **디자인**: 기존 `index.html`의 Wanted Montage 토큰을 그대로 승계하고 차트·필터·테이블까지 확장.
  라이트/다크 모드 양쪽 지원 (`prefers-color-scheme`).
  상승 = `--status-negative`(적색 계열은 한국 관행상 상승), 하락 = `--status-positive`… **가 아니라**, 혼동을 피해 **상승/하락을 화살표 + 명시적 부호(+/-)로** 표기하고 색은 primary/neutral만 사용한다.
- **반응형**: 데스크톱 2열 → 모바일 1열. 표는 가로 스크롤 컨테이너로 감싼다.
- **접근성**: 차트에 `role="img"` + `aria-label` 요약 제공, 표는 실제 `<table>` 시맨틱 유지.

### 5.5 파일 구성

```
sesac-web/
├── index.html              # 대시보드 (전면 교체)
├── profile.html            # 기존 자기소개 (분리 보존)
├── build_data.py           # CSV → JSON 집계 (표준 라이브러리만)
├── analyze.py              # 기존 유지
├── data/dashboard.json     # 생성물 (커밋)
├── assets/dashboard.css
├── assets/dashboard.js
├── seoul-apt-latest.csv
└── PRD.md
```

> 기존 자기소개 콘텐츠는 삭제하지 않고 `profile.html`로 옮겨 보존한다.

---

## 6. 성공 기준

| # | 기준 | 검증 방법 |
|---|---|---|
| 1 | 페이지 로드 후 **3초 내** 첫 화면 렌더 | JSON 50KB 이하 확인 |
| 2 | 화면의 모든 숫자가 CSV 재집계 결과와 **일치** | `build_data.py` 산출값 ↔ 화면 대조 |
| 3 | "서울 평균 대비" 문구가 **어디에도 없음** (§2.1) | 텍스트 검색 |
| 4 | 표본 10건 미만 단지가 랭킹에 **노출되지 않음** | 필터 조합 테스트 |
| 5 | 라이트/다크 모드 양쪽에서 글자가 읽힘 | 양 모드 스크린샷 |
| 6 | 모바일 폭(375px)에서 **가로 스크롤 없음** | 뷰포트 테스트 |
| 7 | GitHub Pages 배포 URL이 200 응답 | curl |

---

## 7. 범위 밖 (이번에 하지 않음)

- 전세·월세 (사용자가 매매만 선택)
- 강북구 외 구의 심층 페이지 (강북구 고정)
- 지도 시각화 (좌표 데이터 없음)
- 개별 거래 단위 탐색 테이블 (동·단지 랭킹까지가 합의된 깊이)
- 가격 예측·추천 (데이터가 12개월뿐이라 근거 부족)
